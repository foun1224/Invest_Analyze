[project]
name = "chipflow"
version = "0.1.0"
description = "台股籌碼面每日自動分析管線"
requires-python = ">=3.10"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
